import React from "react";
import Layout from "@views/Layout";

interface Props {}

const NewClient: React.FC<Props> = () => {
  return <Layout title="New Client"></Layout>;
};

export default NewClient;
